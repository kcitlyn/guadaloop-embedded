# `vehical_control_unit` — superseded

An earlier VCU project, from before the directory name was corrected to
`vehicle_control_unit` (`dev` @ `1de0079`, "fixed spelling of vehicle in project
name").

**Use `firmware/vehicle_control_unit/` instead.** This copy is plain CubeMX
boilerplate; the properly-spelled one has actual peripheral setup. The two `.ioc`
files are byte-identical, so nothing was lost in the rename.

Kept only so the branch's contents are fully represented. Nothing here needs to
be maintained.
